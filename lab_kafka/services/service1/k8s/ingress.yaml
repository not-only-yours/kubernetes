apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-ingress
spec:
  rules:
    - http:
        paths:
          - path: /api/service3
            pathType: Prefix
            backend:
              service:
                name: client3-service
                port:
                  number: 80
